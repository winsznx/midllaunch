# Midl Network (Staging)
MIDL_RPC_URL="https://rpc.staging.midl.xyz"
FACTORY_ADDRESS=""  # Deployed LaunchFactory address

# PostgreSQL (Railway auto-injects DATABASE_URL when PG plugin linked)
DATABASE_URL="postgresql://user:password@localhost:5432/midllaunch"

# Redis (Railway auto-injects REDIS_URL when Redis plugin linked)
REDIS_URL="redis://localhost:6379"

# API
PORT=4000
CORS_ORIGIN="http://localhost:3002"

# WebSocket
WS_PORT=8080

# Indexer
START_BLOCK=5553
POLL_INTERVAL_MS=5000

# IPFS (Pinata) — used by backend if server-side uploads needed
PINATA_API_KEY=""
PINATA_API_SECRET=""
